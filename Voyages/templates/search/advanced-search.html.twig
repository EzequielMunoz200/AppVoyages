{% extends 'base.html.twig' %}

{% block title %}{{parent()}} - Recherche avancée {% endblock %}
{% block stylesheets %}
{# <link href="{{ asset('css/search.css') }}" type="text/css" rel="stylesheet" /> #}
{% endblock %}
{% block body %}
<div class="d-flex justify-content-center align-content-center">
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-dark btn-sm pull-right">{{ button_label|default('Send') }}</button>
    {{ form_end(form) }}
</div>

<style>
    form {
        margin-top: 100px;
    }
</style>


{% endblock %}